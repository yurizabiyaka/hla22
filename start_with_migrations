#!/bin/bash

docker-compose pull goose
docker-compose up -d --build
docker-compose run --rm goose mysql "dbuser:dbpass@tcp(mariadb:3306)/labonedb" up
docker-compose run -d --name lab_one_backend_run  --service-ports --entrypoint lab_one_backend lab_one_backend 
